# Copy this to .env.development / .env.staging / .env.production
# and fill in the correct URL for each environment.
# IMPORTANT: never commit .env.* files with real URLs to a public repo.

VITE_API_BASE=http://localhost:3000/v1
